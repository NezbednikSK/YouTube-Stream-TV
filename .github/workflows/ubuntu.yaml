name: build
on:
  push:
    branches: [ master ]
  schedule:
    - cron: "30 * * * *"
jobs:
  test:
    runs-on: ubuntu-18.04
    name: build
    steps:
      - name: test1
        run: "whoami"
      - name: test2
        run: "uname -a"
      - name: test3
        run: "wget"
      - name: test4
        run: "curl"
